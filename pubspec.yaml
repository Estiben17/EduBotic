name: ava_platform
description: "proyecto de una plataforma web tipo AVA la cual va a permitir el aprendizaje de la robotica educativa"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.14.0
  provider: ^6.1.1
  percent_indicator: ^4.2.5
  flutter_svg: ^2.0.9

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    